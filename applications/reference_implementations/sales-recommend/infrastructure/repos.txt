# Repo list for the wiki generator.
# One repository URL per line. Blank lines and lines starting with '#' are ignored.
# When this file is uploaded to s3://<wiki-src-bucket>/config/repos.txt, the
# dispatch Lambda starts one CodeBuild build per URL. Re-uploading reprocesses
# the whole list (each repo's profile is overwritten in place).
#
# Add new repositories by appending a line and re-running the deploy script,
# or by uploading this file directly:
#   aws s3 cp repos.txt s3://<wiki-src-bucket>/config/repos.txt

# --- Repositories to profile ---
https://github.com/finos/opengris-scaler
https://github.com/aws-samples/quant-research
https://github.com/aws-samples/quant-trading
https://github.com/aws-samples/trading-latency-benchmark
