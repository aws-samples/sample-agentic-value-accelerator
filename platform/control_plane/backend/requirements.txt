# Control Plane Backend Dependencies

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.35
alembic==1.14.0
# psycopg2-binary==2.9.9  # Optional for PostgreSQL; SQLite works without it

# AWS Services
# >=1.43.0 required for bedrock-agentcore-control policy-engine APIs
# (list_policy_engines / create_policy_engine / create_gateway_target)
boto3>=1.43.0
botocore>=1.43.0

# Authentication
python-jose[cryptography]==3.3.0
PyJWT[crypto]==2.8.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0

# Outbound HTTP
# Declared explicitly because it is a PRODUCTION dependency that was only arriving
# transitively, via moto and responses in the Testing block below. core/security.py
# imports it on the token-verification path, as do the multicloud connector and spend
# aggregator services. A single requirements.txt installs test libraries into the
# image, so this worked - but it meant removing a test dependency would have broken
# JWT authentication, and nothing in the file said so.
# >=2.32.0 for the redirect handling this code relies on: SessionRedirectMixin
# .rebuild_auth drops Authorization on a cross-host redirect, which is the behaviour
# core/safe_fetch.py had to reimplement for the urllib call sites that lack it.
requests==2.32.5

# Validation
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.1.1
jsonschema==4.21.1

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
hypothesis==6.92.1
moto==5.0.0
httpx==0.26.0

# Code Quality
black==24.1.1
flake8==7.0.0
mypy==1.8.0

# Utilities
python-dateutil==2.8.2
click==8.1.7
Markdown==3.10.3
