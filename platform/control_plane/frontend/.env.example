# API URL - leave empty to use Vite proxy (recommended, ensures consistent behavior)
# The Vite proxy forwards /api/* requests to the backend automatically
VITE_API_URL=

VITE_COGNITO_USER_POOL_ID=local-dev
VITE_COGNITO_CLIENT_ID=local-dev
VITE_COGNITO_REGION=us-east-1
