# API URL - leave empty to use Vite proxy (recommended for Windows to avoid connection resets)
# Mac/Linux can use http://localhost:8000 directly
VITE_API_URL=http://localhost:8000

VITE_COGNITO_USER_POOL_ID=us-east-1_xxxxx
VITE_COGNITO_CLIENT_ID=xxxxx
VITE_COGNITO_REGION=us-east-1
